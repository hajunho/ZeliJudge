# 📘 [ZeliJudge Junior #059] 자리(Index)도 같아야 할까, 존재(in)만 하면 될까?

---

## 1. 인덱스 일치와 포함 여부 분기

* **스트라이크 판정**: `guess[i] == secret[i]` (같은 번호 칸에 같은 글자가 있는가?)
* **볼 판정**: `guess[i] in secret` (자리는 틀렸지만 비밀 글자 안에 포함되어 있는가?)

`if - elif` 문을 사용하면 스트라이크로 판정된 글자가 볼로 중복 계산되는 것을 완벽하게 방지할 수 있습니다!
